services:
  rhizome:
    image: ghcr.io/cstanze/rhizome:latest
    container_name: rhizome
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=UTC