services:
  rhizome:
    image: ghcr.io/cstanze/rhizome:latest
    container_name: rhizome
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=UTC